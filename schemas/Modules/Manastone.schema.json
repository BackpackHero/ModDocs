{
    "type": "object",
    "required": [
        "max_mana"
    ],
    "properties": {
        "starting_mana": {
            "type": "integer"
        },
        "max_mana": {
            "type": "integer"
        }
    },
    "defaultSnippets":[
        {
            "label":"Manastone",
            "body":{
                "starting_mana":  "^${1:3}",
                "max_mana":  "^${2:3}"
            }
        }
    ]
}